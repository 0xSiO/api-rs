[package]
name = "api"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
axum = { version = "0.4.0", default-features = false, features = ["http2", "json", "tower-log"] }
tokio = { version = "*", features = ["macros", "rt-multi-thread"] }
